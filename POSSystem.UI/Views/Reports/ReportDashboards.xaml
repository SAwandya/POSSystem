<Window x:Class="POSSystem.UI.Views.Reports.ReportDashboards"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        
        Title="Analytics and Reports Dashboard"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#FF0F0F1A"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Color Resources -->
        <SolidColorBrush x:Key="PrimaryPurple" Color="#FF8B5DFF"/>
        <SolidColorBrush x:Key="SecondaryBlue" Color="#FF3D8BFF"/>
        <SolidColorBrush x:Key="AccentCyan" Color="#FF00D4FF"/>
        <SolidColorBrush x:Key="AccentPink" Color="#FFFF5CAA"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#FF00E096"/>
        <SolidColorBrush x:Key="WarningYellow" Color="#FFFFD166"/>
        <SolidColorBrush x:Key="ErrorRed" Color="#FFFF5E7C"/>

        <!-- Dark Theme -->
        <SolidColorBrush x:Key="DarkBackground" Color="#FF0F0F1A"/>
        <SolidColorBrush x:Key="DarkCard" Color="#FF1A1A2E"/>
        <SolidColorBrush x:Key="DarkSurface" Color="#FF252540"/>
        <SolidColorBrush x:Key="DarkBorder" Color="#FF3A3A5E"/>

        <!-- Light Text -->
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FFB0B0D0"/>
        <SolidColorBrush x:Key="TextTertiary" Color="#FF8888AA"/>

        <!-- Light Backgrounds with Alpha -->
        <SolidColorBrush x:Key="PurpleLight20" Color="#208B5DFF"/>
        <SolidColorBrush x:Key="BlueLight20" Color="#203D8BFF"/>
        <SolidColorBrush x:Key="GreenLight20" Color="#2000E096"/>
        <SolidColorBrush x:Key="YellowLight20" Color="#20FFD166"/>
        <SolidColorBrush x:Key="CyanLight20" Color="#2000D4FF"/>
        <SolidColorBrush x:Key="PinkLight20" Color="#20FF5CAA"/>
        <SolidColorBrush x:Key="WhiteLight10" Color="#10FFFFFF"/>
        <SolidColorBrush x:Key="WhiteLight20" Color="#20FFFFFF"/>

        <!-- Gradients -->
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF8B5DFF" Offset="0"/>
            <GradientStop Color="#FF3D8BFF" Offset="1"/>
        </LinearGradientBrush>

        <!-- Window Controls Style -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#30FFFFFF"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#50FFFFFF"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Report Card Styles -->
        <Style x:Key="ReportCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource DarkCard}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="16"/>
        </Style>

        <Style x:Key="KPICard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource DarkSurface}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="16,12"/>
        </Style>

        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>

        <Style x:Key="MetricLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkSurface}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryPurple}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Time Period Selector Style -->
        <Style x:Key="TimePeriodButton" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="8,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkSurface}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryPurple}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryPurple}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Custom Title Bar -->
        <Border Height="40" 
                Background="{StaticResource DarkCard}" 
                VerticalAlignment="Top"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo & Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                    <Border Width="32" Height="32" CornerRadius="8" 
                            Background="{StaticResource PrimaryGradient}" 
                            Margin="0,0,10,0">
                        <TextBlock Text="📊" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="ANALYTICS and REPORTS" 
                                 FontSize="16" 
                                 FontWeight="Bold" 
                                 Foreground="{StaticResource TextPrimary}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Window Controls -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Background Effects -->
        <Grid Margin="0,40,0,0">
            <Ellipse Width="500" Height="500" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-250,-250,0,0">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#408B5DFF" Offset="0"/>
                        <GradientStop Color="#00000000" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>
            <Ellipse Width="350" Height="350" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-150,-150">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#403D8BFF" Offset="0"/>
                        <GradientStop Color="#00000000" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>

            <!-- Main Content Area -->
            <Grid Margin="15,15,15,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header with Time Period Selector -->
                <Border Grid.Row="0" 
                        Background="{StaticResource DarkSurface}" 
                        CornerRadius="10"
                        Padding="20" 
                        Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Time Period Selector -->
                        <StackPanel Grid.Column="0" 
                                    Orientation="Horizontal" 
                                    VerticalAlignment="Center">
                            <TextBlock Text="Period:" 
                                     FontSize="13" 
                                     Foreground="{StaticResource TextSecondary}"
                                     Margin="0,0,10,0"
                                     VerticalAlignment="Center"/>
                            <RadioButton x:Name="rbToday" Content="Today" GroupName="TimePeriod" Style="{StaticResource TimePeriodButton}" Margin="0,0,5,0" IsChecked="True"/>
                            <RadioButton x:Name="rbWeek" Content="Week" GroupName="TimePeriod" Style="{StaticResource TimePeriodButton}" Margin="5,0"/>
                            <RadioButton x:Name="rbMonth" Content="Month" GroupName="TimePeriod" Style="{StaticResource TimePeriodButton}" Margin="5,0"/>
                            <RadioButton x:Name="rbQuarter" Content="Quarter" GroupName="TimePeriod" Style="{StaticResource TimePeriodButton}" Margin="5,0"/>
                            <RadioButton x:Name="rbYear" Content="Year" GroupName="TimePeriod" Style="{StaticResource TimePeriodButton}" Margin="5,0"/>
                        </StackPanel>

                        <!-- Actions -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="btnExport" 
                                    Style="{StaticResource FilterButtonStyle}" 
                                    Margin="0,0,10,0"
                                    Click="ExportButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📤" Margin="0,0,6,0"/>
                                    <TextBlock Text="Export"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="btnPrint" 
                                    Style="{StaticResource FilterButtonStyle}" 
                                    Margin="0,0,10,0"
                                    Click="PrintButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🖨️" Margin="0,0,6,0"/>
                                    <TextBlock Text="Print"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="btnRefresh" 
                                    Style="{StaticResource FilterButtonStyle}"
                                    Background="{StaticResource PrimaryPurple}"
                                    Foreground="White"
                                    Click="RefreshButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🔄" Margin="0,0,6,0"/>
                                    <TextBlock Text="Refresh"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Main Content with Scroll -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid>
                        <!-- Key Metrics Row -->
                        <Grid Margin="0,0,0,15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Key Performance Indicators -->
                            <UniformGrid Grid.Row="0" Columns="6" Margin="0,0,0,15">
                                <!-- Total Revenue -->
                                <Border Style="{StaticResource KPICard}">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <Border Width="32" Height="32" CornerRadius="6" Background="{StaticResource GreenLight20}">
                                                <TextBlock Text="💰" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Revenue" Style="{StaticResource MetricLabelStyle}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock x:Name="txtTotalRevenue" Text="$24,580.50" Style="{StaticResource MetricValueStyle}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="▲ 12.5%" FontSize="10" Foreground="{StaticResource SuccessGreen}" FontWeight="SemiBold"/>
                                            <TextBlock Text=" vs last month" FontSize="10" Foreground="{StaticResource TextTertiary}" Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- Total Orders -->
                                <Border Style="{StaticResource KPICard}">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <Border Width="32" Height="32" CornerRadius="6" Background="{StaticResource PurpleLight20}">
                                                <TextBlock Text="📦" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Orders" Style="{StaticResource MetricLabelStyle}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock x:Name="txtTotalOrders" Text="1,248" Style="{StaticResource MetricValueStyle}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="▲ 8.2%" FontSize="10" Foreground="{StaticResource SuccessGreen}" FontWeight="SemiBold"/>
                                            <TextBlock Text=" vs last month" FontSize="10" Foreground="{StaticResource TextTertiary}" Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- Average Order Value -->
                                <Border Style="{StaticResource KPICard}">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <Border Width="32" Height="32" CornerRadius="6" Background="{StaticResource BlueLight20}">
                                                <TextBlock Text="📊" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Avg Order" Style="{StaticResource MetricLabelStyle}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock x:Name="txtAvgOrderValue" Text="$196.80" Style="{StaticResource MetricValueStyle}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="▲ 4.1%" FontSize="10" Foreground="{StaticResource SuccessGreen}" FontWeight="SemiBold"/>
                                            <TextBlock Text=" vs last month" FontSize="10" Foreground="{StaticResource TextTertiary}" Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- Profit Margin -->
                                <Border Style="{StaticResource KPICard}">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <Border Width="32" Height="32" CornerRadius="6" Background="{StaticResource YellowLight20}">
                                                <TextBlock Text="📈" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Profit Margin" Style="{StaticResource MetricLabelStyle}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock x:Name="txtProfitMargin" Text="32.5%" Style="{StaticResource MetricValueStyle}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="▲ 2.3%" FontSize="10" Foreground="{StaticResource SuccessGreen}" FontWeight="SemiBold"/>
                                            <TextBlock Text=" vs last month" FontSize="10" Foreground="{StaticResource TextTertiary}" Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- Top Selling Category -->
                                <Border Style="{StaticResource KPICard}">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <Border Width="32" Height="32" CornerRadius="6" Background="{StaticResource CyanLight20}">
                                                <TextBlock Text="🏆" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Top Category" Style="{StaticResource MetricLabelStyle}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock x:Name="txtTopCategory" Text="Electronics" Style="{StaticResource MetricValueStyle}" FontSize="20"/>
                                        <TextBlock Text="42% of sales" FontSize="10" Foreground="{StaticResource TextTertiary}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Customer Satisfaction -->
                                <Border Style="{StaticResource KPICard}">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <Border Width="32" Height="32" CornerRadius="6" Background="{StaticResource PinkLight20}">
                                                <TextBlock Text="⭐" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Satisfaction" Style="{StaticResource MetricLabelStyle}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock x:Name="txtCustomerSatisfaction" Text="4.8/5.0" Style="{StaticResource MetricValueStyle}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="94%" FontSize="10" Foreground="{StaticResource SuccessGreen}" FontWeight="SemiBold"/>
                                            <TextBlock Text=" positive" FontSize="10" Foreground="{StaticResource TextTertiary}" Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>

                            <!-- Charts Row -->
                            <UniformGrid Grid.Row="1" Columns="2" Margin="0,0,0,15">
                                <!-- Sales Trend Chart -->
                                <Border Style="{StaticResource ReportCard}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                                            <TextBlock Text="Sales Trend" Style="{StaticResource SectionHeaderStyle}"/>
                                            <ComboBox x:Name="cmbChartPeriod" 
                                                      Margin="12,0,0,0" Width="100" Height="28" 
                                                      Background="{StaticResource DarkSurface}"
                                                      Foreground="{StaticResource DarkBackground}"
                                                      BorderBrush="{StaticResource DarkBorder}"
                                                      FontSize="11"
                                                      SelectionChanged="ChartPeriod_SelectionChanged">
                                                <ComboBoxItem Content="Daily" IsSelected="True"/>
                                                <ComboBoxItem Content="Weekly"/>
                                                <ComboBoxItem Content="Monthly"/>
                                            </ComboBox>
                                        </StackPanel>

                                        <!-- OxyPlot Sales Chart -->
                                        <oxy:PlotView Grid.Row="1" 
                                                      x:Name="SalesChart" 
                                                      Background="DarkBlue"
                                                      Margin="0,8,0,0"/>
                                    </Grid>
                                </Border>

                                <!-- Revenue by Category -->
                                <Border Style="{StaticResource ReportCard}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="Revenue by Category" Style="{StaticResource SectionHeaderStyle}"/>

                                        <!-- OxyPlot Pie Chart -->
                                        <oxy:PlotView Grid.Row="1" 
                                                      x:Name="CategoryChart" 
                                                      Background="Transparent"
                                                      Margin="0,8,0,0"/>
                                    </Grid>
                                </Border>
                            </UniformGrid>

                            <!-- Data Tables Row -->
                            <UniformGrid Grid.Row="2" Columns="2" Margin="0,0,0,15">
                                <!-- Top Products Table -->
                                <Border Style="{StaticResource ReportCard}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                                            <TextBlock Text="Top Selling Products" Style="{StaticResource SectionHeaderStyle}"/>
                                            <Button x:Name="btnViewAllProducts" 
                                                    Style="{StaticResource FilterButtonStyle}" 
                                                    Margin="12,0,0,0"
                                                    Padding="8,4"
                                                    Click="ViewAllProducts_Click">
                                                <TextBlock Text="View All →" FontSize="11"/>
                                            </Button>
                                        </StackPanel>

                                        <DataGrid x:Name="dgTopProducts"
                                                  Grid.Row="1" 
                                                  AutoGenerateColumns="False"
                                                  HeadersVisibility="Column"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  RowBackground="Transparent"
                                                  AlternatingRowBackground="{StaticResource WhiteLight10}"
                                                  Foreground="{StaticResource TextPrimary}"
                                                  FontSize="12"
                                                  CanUserAddRows="False"
                                                  IsReadOnly="True">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="#" Width="35" Binding="{Binding Rank}"/>
                                                <DataGridTextColumn Header="Product" Width="180" Binding="{Binding Name}"/>
                                                <DataGridTextColumn Header="Category" Width="100" Binding="{Binding Category}"/>
                                                <DataGridTextColumn Header="Sales" Width="90" Binding="{Binding Sales, StringFormat=C}"/>
                                                <DataGridTextColumn Header="Units" Width="70" Binding="{Binding Units}"/>
                                                <DataGridTextColumn Header="Growth" Width="80">
                                                    <DataGridTextColumn.ElementStyle>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{Binding GrowthColor}"/>
                                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        </Style>
                                                    </DataGridTextColumn.ElementStyle>
                                                    <DataGridTextColumn.Binding>
                                                        <Binding Path="Growth" StringFormat="{}{0}%"/>
                                                    </DataGridTextColumn.Binding>
                                                </DataGridTextColumn>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </Grid>
                                </Border>

                                <!-- Recent Transactions -->
                                <Border Style="{StaticResource ReportCard}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                                            <TextBlock Text="Recent Transactions" Style="{StaticResource SectionHeaderStyle}"/>
                                            <Button x:Name="btnViewAllTransactions" 
                                                    Style="{StaticResource FilterButtonStyle}" 
                                                    Margin="12,0,0,0"
                                                    Padding="8,4"
                                                    Click="ViewAllTransactions_Click">
                                                <TextBlock Text="View All →" FontSize="11"/>
                                            </Button>
                                        </StackPanel>

                                        <DataGrid x:Name="dgRecentTransactions"
                                                  Grid.Row="1" 
                                                  AutoGenerateColumns="False"
                                                  HeadersVisibility="Column"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  RowBackground="Transparent"
                                                 
                                                  Foreground="{StaticResource TextPrimary}"
                                                  FontSize="12"
                                                  CanUserAddRows="False"
                                                  IsReadOnly="True">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="Time" Width="80" Binding="{Binding Time, StringFormat=HH:mm}"/>
                                                <DataGridTextColumn Header="Invoice" Width="100" Binding="{Binding InvoiceNumber}"/>
                                                <DataGridTextColumn Header="Customer" Width="130" Binding="{Binding Customer}"/>
                                                <DataGridTextColumn Header="Items" Width="70" Binding="{Binding Items}"/>
                                                <DataGridTextColumn Header="Amount" Width="90" Binding="{Binding Amount, StringFormat=C}"/>
                                                <DataGridTemplateColumn Header="Status" Width="90">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <Border CornerRadius="10" Padding="5,2" HorizontalAlignment="Left"
                                                                    Background="{Binding StatusBackground}">
                                                                <TextBlock Text="{Binding Status}" 
                                                                           FontSize="10" 
                                                                           FontWeight="SemiBold"
                                                                           Foreground="{Binding StatusColor}"/>
                                                            </Border>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </Grid>
                                </Border>
                            </UniformGrid>

                            <!-- Quick Report Cards -->
                            <UniformGrid Grid.Row="3" Columns="3">
                                <!-- Stock Report Card -->
                                <Border Style="{StaticResource KPICard}" Margin="4">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Border Width="40" Height="40" CornerRadius="8" Background="{StaticResource BlueLight20}">
                                                <TextBlock Text="📦" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Stock Report" 
                                                     FontSize="13" 
                                                     FontWeight="SemiBold"
                                                     Foreground="{StaticResource TextPrimary}"
                                                     Margin="10,0,0,0"
                                                     VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="Inventory levels, low stock alerts" 
                                                 FontSize="10" 
                                                 Foreground="{StaticResource TextSecondary}"
                                                 Margin="0,0,0,10"/>
                                        <Button x:Name="btnViewStockReport" 
                                                Style="{StaticResource FilterButtonStyle}"
                                                Padding="12,6"
                                                Click="ViewStockReport_Click">
                                            <TextBlock Text="View Report →" FontSize="11"/>
                                        </Button>
                                    </StackPanel>
                                </Border>

                                <!-- Customer Report Card -->
                                <Border Style="{StaticResource KPICard}" Margin="4">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Border Width="40" Height="40" CornerRadius="8" Background="{StaticResource PurpleLight20}">
                                                <TextBlock Text="👥" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Customer Report" 
                                                     FontSize="13" 
                                                     FontWeight="SemiBold"
                                                     Foreground="{StaticResource TextPrimary}"
                                                     Margin="10,0,0,0"
                                                     VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="Customer demographics, purchase history" 
                                                 FontSize="10" 
                                                 Foreground="{StaticResource TextSecondary}"
                                                 Margin="0,0,0,10"/>
                                        <Button x:Name="btnViewCustomerReport" 
                                                Style="{StaticResource FilterButtonStyle}"
                                                Padding="12,6"
                                                Click="ViewCustomerReport_Click">
                                            <TextBlock Text="View Report →" FontSize="11"/>
                                        </Button>
                                    </StackPanel>
                                </Border>

                                <!-- Financial Report Card -->
                                <Border Style="{StaticResource KPICard}" Margin="4">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Border Width="40" Height="40" CornerRadius="8" Background="{StaticResource YellowLight20}">
                                                <TextBlock Text="💰" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="Financial Report" 
                                                     FontSize="13" 
                                                     FontWeight="SemiBold"
                                                     Foreground="{StaticResource TextPrimary}"
                                                     Margin="10,0,0,0"
                                                     VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="Profit and loss, cash flow analysis" 
                                                 FontSize="10" 
                                                 Foreground="{StaticResource TextSecondary}"
                                                 Margin="0,0,0,10"/>
                                        <Button x:Name="btnViewFinancialReport" 
                                                Style="{StaticResource FilterButtonStyle}"
                                                Padding="12,6"
                                                Click="ViewFinancialReport_Click">
                                            <TextBlock Text="View Report →" FontSize="11"/>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                        </Grid>
                    </Grid>
                </ScrollViewer>

                <!-- Footer -->
                <Border Grid.Row="2" 
                        Background="{StaticResource DarkCard}" 
                        CornerRadius="8" 
                        Margin="0,15,0,0"
                        Height="40">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Report Info -->
                        <WrapPanel Grid.Column="0" VerticalAlignment="Center" Margin="12,0">
                            <TextBlock Text="Last Updated: " 
                                     FontSize="10" 
                                     Foreground="{StaticResource TextSecondary}"/>
                            <TextBlock x:Name="txtLastUpdated" 
                                     Text="Today, 10:30 AM" 
                                     FontSize="10" 
                                     FontWeight="Medium"
                                     Foreground="{StaticResource TextPrimary}"
                                     Margin="2,0,15,0"/>

                            <TextBlock Text="Data Range: " 
                                     FontSize="10" 
                                     Foreground="{StaticResource TextSecondary}"/>
                            <TextBlock x:Name="txtDataRange" 
                                     Text="Jan 1 - Dec 31, 2024" 
                                     FontSize="10" 
                                     FontWeight="Medium"
                                     Foreground="{StaticResource TextPrimary}"
                                     Margin="2,0,15,0"/>

                            <TextBlock Text="Generated By: " 
                                     FontSize="10" 
                                     Foreground="{StaticResource TextSecondary}"/>
                            <TextBlock Text="POS System v2.0" 
                                     FontSize="10" 
                                     FontWeight="Medium"
                                     Foreground="{StaticResource TextPrimary}"
                                     Margin="2,0,0,0"/>
                        </WrapPanel>

                        <!-- Footer Actions -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                            <Button x:Name="btnHelp" 
                                  Style="{StaticResource FilterButtonStyle}" 
                                  Padding="8,4"
                                  Margin="0,0,6,0"
                                  Click="HelpButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="❓" FontSize="11" Margin="0,0,4,0"/>
                                    <TextBlock Text="Help" FontSize="10"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="btnSchedule" 
                                  Style="{StaticResource FilterButtonStyle}" 
                                  Padding="8,4"
                                  Click="ScheduleButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⏰" FontSize="11" Margin="0,0,4,0"/>
                                    <TextBlock Text="Schedule" FontSize="10"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>